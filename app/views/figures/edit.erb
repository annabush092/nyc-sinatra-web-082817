<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Figure</title>
  </head>
  <body>
    <form action="/figures/<%=@figure.id%>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <h3>Name:</h3>
        <input type="text" name="figure[name]" value="<%=@figure.name%>" id="figure_name"></br>
      <h3>Select Landmarks to Associate:</h3>
        <%Landmark.all.each do |landmark|%>
          <input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id= "landmark_<%=landmark.id%>" <%='checked' if @figure.landmarks.include?(landmark)%>><%=landmark.name%></input></br>
        <%end%>
      <h3>And/or Create Landmarks to Associate:</h3>
      <label> Name </label>
        <input type="text" name="landmark[name]" id="new_landmark"></br>
      <label> Year Completed </label>
        <input type="text" name="landmark[year_completed]" id="landmark_year_completed"></br>
      <h3>Select Titles to Associate:</h3>
        <%Title.all.each do |title|%>
          <input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" id="title_<%=title.id%>"<%='checked' if @figure.titles.include?(title)%>><%=title.name%></input></br>
        <% end %>
      <h3>And/or Create Titles to Associate:</h3>
      <label> Name </label>
        <input type="text" name="title[name]" id="title_name"></br>

      <button type="submit" id="submit">Edit Figure</button>
    </form>
  </body>
</html>
