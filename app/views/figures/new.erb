<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>New Figure</title>
  </head>
  <body>
    <form action="/figures" method="post">
      <h3>New Figure</h3>
      <label>Name:</label>
        <input type="text" name="figure[name]" id="figure_name"></br>
      <h3>Select Landmarks to Associate:</h3>
        <%Landmark.all.each do |landmark|%>
          <input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%=landmark.id%>"><%=landmark.name%></input></br>
        <%end%></br>

      <h3>And/or Create a New Landmark to Associate:</h3>
        <p>Name:</p>
          <input type="text" name="landmark[name]" id="new_landmark">
        <p>Year Completed:</p>
          <input type="text" name="landmark[year_completed]" id="landmark_year_completed">

      <h3>Select Titles to Associate:</h3>
        <%Title.all.each do |title|%>
          <input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" id="title_<%=title.id%>"><%=title.name%></input></br>
        <%end%></br>

      <h3>And/or Create a New Title to Associate:</h3>
        <p>Name:</p>
          <input type="text" name="title[name]" id="new_title">
        <p>Year Completed:</p>
          <input type="text" name="title[year_completed]" id="title_year_completed">
      <button type="submit" id="submit">Create New Figure</button>
    </form>
  </body>
</html>
